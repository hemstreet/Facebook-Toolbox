(function ($) {

    // running before window is ready seems to be working,
    // i'm going to leave this code here incase we start running
    // into startup issues

    //$(window).on('ready', function() {
        facebook.init();
    //});

})(jQuery);

